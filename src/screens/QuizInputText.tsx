export default function InputText() {
    return (
        <div className="input-text-wrapper">
            input text wrapper (translate page) {/* page responsible for all inputs, mainly will be used for translations */}
        </div>
    )
}